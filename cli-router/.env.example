# Never commit real keys. The key stays in the local Node process.
# Obtain an official TypeSafe key at https://typesafe.ai/
TYPESAFE_API_KEY=
# JEV_API_KEY is accepted as an alias and takes precedence if both are set.
