# Application
spring.application.name=ai-model-picker
# Render (and most PaaS) injects PORT; default to 8080 locally/in Docker
server.port=${PORT:8080}

# CORS — comma-separated origins, wildcards allowed (e.g. https://*.vercel.app)
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000}

# Admin API key — when set, /api/admin/** requires X-Admin-Key header
app.admin.api-key=${ADMIN_API_KEY:}

# Per-request API logging (method, uri, origin, status, duration, bytes)
app.log-requests=${LOG_REQUESTS:true}

# MongoDB — URI must include the database name in its path, e.g.
# mongodb+srv://user:pass@cluster.mongodb.net/ai_model_picker?appName=Cluster24328
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/ai_model_picker}

# Logging
logging.level.com.aimodelpicker=DEBUG
